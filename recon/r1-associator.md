## Vault (Codebase) Connections

- **`backends/model_converter/tdict.py` ↔ cartridge/ROM header pattern**: The `.tdict` format's `magic_number=4346464`, 64-byte block alignment, three-region layout (`extra_head` / `weights_json` / `metadata_json`) and `tdict_format_version` field mirror the structure of a classic NES ROM header, ELF relocatable object, or IFF chunked format. The pre-baked `causal_mask` / `aux_output_conv` constants and the `ff._split_1/2` tensor surgery in `convert_model.py` is exactly the "pre-link executable" step — `safetensors` is source code, `.tdict` is a pre-linked `.exe`. The format is *stricter* than `safetensors` (it asserts alignment, version, and a `ctdict_version` ABI tag at read time), which is why the legacy-detection error in `init_read` exists.

- **`scripts/lib/backend_client.py` + `py_vue_bridge.js` ↔ G-code / IRC / SMTP line protocol**: The five prefixes (`b2py`, `py2b`, `sdbk`, `utds`, `adlg`, `alrt`) function like IRC verbs (`PRIVMSG`, `NOTICE`) or SMTP commands (`HELO`, `MAIL FROM`). State updates encode the path with `___U_P_D_A_T_E___` as a delimiter — the same trick HTTP uses with `:` in headers and that SMTP uses with `:` after the verb. The "structured footer" `__converted_model_data__{...}` injected into the data stream is exactly an email-style structured header (DKIM-Signature, X-Mailer). The `Unbuffered` wrapper class at `diffusionbee_backend.py:76` is the tell: this protocol only works because stdout is forced into raw/line mode, the way serial terminals need `stty raw`.

- **`scripts/setup_models.py` + `install_hf_model.py` + `electron_app/src/AssetsManager.vue` ↔ package-manager convergence**: All three paths write into the *same* `~/.diffusionbee/downloaded_assets.json` registry but speak three different protocols — `huggingface_hub` (Python), `request` + MD5 (Electron main), and a tdict post-process on top. The relationship is `apt` / `dpkg` / `apt-get source`: different frontends, identical database. The two MD5-checking paths (SD with checksum, FLUX.2 with `skip_checksum: true` + HF bearer token) reproduce the **PGP-signature vs HTTPS-cert** trust split: closed ecosystem gets content-hash integrity, open ecosystem gets only authentication.

- **`scripts/bundle_default_models.js` + `electron_app/src/seed_bundled_models.js` ↔ "baked-in base image" pattern**: A build-time stager (Node, reads `manifest.json`) plus a first-launch seeder (Node, copies bundled files into `~/.diffusionbee/`) is the Electron-app version of a Docker `FROM` layer or a phone's preinstalled apps. The `is_stock_model: true` flag is the equivalent of a `PreInstalledApp` system property on Android — used by `BasicSDApplet.vue` to prefer the bundled default over downloads (`if (!current || current === 'Default_SD1.5' ...)`). This is the same pattern as Chrome shipping with Google as the default search engine, or macOS shipping with Safari as the default browser.

- **`backends/stable_diffusion/applets/applets.py` + `backends/stable_diffusion/stable_diffusion/plugins/` ↔ WordPress `add_action` / Photoshop filter model**: Both are reflection-based hook registries, not a graph. The plugin side walks `dir(plugin_object)` for `hook_*` methods (`plugin_system.py:26`), the applet side walks Python class registration (`register_applet`). This is the "loose-coupled behavior composition" pattern from CMS-era web frameworks. The same `BasePlugin` parent reference pattern is used by `InterpolatorHelperPlugin` inside `frame_interpolator.py` to reach back into the parent generator — a parent/child inversion of a typical decorator stack.

- **`electron_app/src/SDManager.vue` ↔ printer spool / single-worker render farm**: `queue.current_group` / `current_job_index` / `groups_todo` is a classic single-worker FIFO with a "current job" pointer and a "ready" watcher that pulls the next item. The `seed += 1234` per-batch increment is the equivalent of a render farm's "same camera, different sample seed" — a 3D artist's batch render pattern (Maya batch render, Blender frame-range render). The `stop_all` is an emergency spool cancel: flush queue, mark all `done`, send `__stop__` to the worker.

- **`electron_app/src/utils/model_selection.js` ↔ hand-tuned Bayesian ranking (IMDb Top 250 / early Google PageRank)**: The `scoreStableDiffusionModel` function is a weighted-keyword `rec` formula with positive/negative bonus tables and a `sortStableDiffusionModelsBestFirst` stable sort with a tiebreaker chain. This is **pre-ML editorial ranking** — the same shape as the IMDb top-250 Bayesian formula, the early PageRank variant, or the App Store's pre-iOS-11 "Editor's Choice" curation. The Apple-Silicon bonus (`+5`) is a fingerprint of the developer's deployment target (macOS-first fork). The fact that `flux` triggers a `-1000` penalty reveals a *one-model-family-at-a-time* design constraint expressed as a feature weight, not an enum.

- **`electron_app/src/utils/ollama_prompt_service.js` ↔ mirror of `model_selection.js`**: The Ollama-side `scoreOllamaModel` reuses the *exact same hand-tuned-keyword shape* with a different lexicon — `qwen`/`gemma`/`llama` as positive keywords, `code`/`moondream`/`embedding` as negatives. This is "the same ranking function ported to a second marketplace." It reveals an *implicit abstraction*: the developers have a `getMachineProfile()`-aware scoring function in their head that they keep re-implementing. It is a missing generalization — like having two near-identical `pom.xml` files in two projects because nobody built a shared library.

- **`electron_app/src/pages/ModelStore.vue` ↔ App Store / F-Droid "Installed vs Browse" pattern**: Two-card-pane layout ("My Models" above, "Available Models" below) with a delete button on installed cards and a download button on available cards is the **iOS App Store / Google Play Store / Steam Library** UX distilled to two words. The `model_metadata_to_str` rendering (`SD_1x float16`) is the iOS App's "Version 1.4.2 · 124 MB" subtitle. The Arabic "النماذج المتاحة" → "Available Models" labels and the `forceRefresh` button on the bottom are the "Refresh Storefront" / pull-to-refresh pattern from app stores.

- **`scripts/compose_txt2img_screenshot.py` + `scripts/verify_doc_screenshots.py` ↔ visual regression test disguised as documentation**: This is the **ArgoCD / Percy.io / Chromatic screenshot diff** pattern, but with PIL instead of Puppeteer. The composition step pastes a real generated image into a captured UI frame; the verification step uses `statistics.pvariance` on sampled pixels to confirm "the gallery region is not blank." It's documentation that *proves itself by computing pixel statistics on a real artifact*. The `ensure_doc_screenshots.sh` orchestrator is a tiny CI pipeline: generate → capture → composite → verify.

- **`backends/stable_diffusion/diffusionbee_backend.py` CLI mode ↔ `git` / `docker` dual-mode binary**: The same Python entrypoint runs as a persistent daemon (stdin/stdout loop, `process_opt`) OR as a one-shot CLI (`argv[1] == 'convert_model'`). `native_functions.js:495` spawns it as the second form, the `b2py t2im` consumer uses it as the first. This is exactly the **`git` (interactive + plumbing) / `docker` (daemon + CLI) / `kubectl` (server + client)** pattern — argv[1] as a command dispatch table.

- **`backends/stable_diffusion/applets/frame_interpolator.py` ↔ "macro / script action" pattern**: The interpolator is a multi-step workflow (capture seed/prompt A → capture seed/prompt B → for i in N: generate weighted interpolation → assemble GIF) registered as a single applet. The `frac_a_latent = np.sqrt(1 - frac)` is a hand-rolled easing function — the same shape as a Photoshop Action or a Blender Python script. The "applet" abstraction is really a *fixed workflow macro* over the underlying single-step `text_to_img` primitive.

- **`backends/stable_diffusion/applets/options.py` (a 346-line raw JSON string) ↔ form-schema DSL**: One giant string literal defines the entire generation form — accordions, sliders, dropdowns, conditional visibility (`include_in: ["advanced"]`). This is the same pattern as `tsconfig.json`, `Jekyll _config.yml`, or `package.json` scripts — a declarative UI description consumed by a generic form renderer (`Form.vue` in `components_bare/`). The dual locations of this schema (`applets/options.py` and `forms/sd_options_adv.json`) hint at an in-progress migration between Python-side and JS-side form definitions.

- **`Homepage.vue` welcome tiles + inspiration chips ↔ Spotify Discover Weekly / Apple first-run tutorial**: The carousel of pre-generated sample images, the `Inspiration samples` heading, and the `useWelcomePrompt(img.prompt)` click handler are a "show what's possible, then one-click into it" pattern. The inspiration chips (`Aurora over fjord` / `Cyberpunk at night`) are reduced-prompt presets — like Apple's example Siri queries or a CMS's "Try these" starter set. The `recent_home_prompts` localStorage list + `recent_prompts` UI dropdown add a "recents" tab that browsers have had since IE5.

- **`hf_auth.js` + `install_hf_model.py` HF_TOKEN ↔ `gh` PAT / `aws-cli` credentials pattern**: Token is read from env, never written to disk, cached only in process memory, gates a specific *feature branch* (`requires_hf_token: true` models). The `getHfTokenSync` / `getHfToken` dual API is the same shape as `gh auth status` / `gh auth login`. The `clearHfTokenCache` is the "logout" function. Crucially, the gated model is only *visible* in the catalog when the token is present (`enrichFlux2Model` returns `null` if token missing) — the credential *unlocks UI affordances*, not just downloads.

## Cross-Domain Bridges

- **G-code (3D printers) ↔ generation queue protocol**: The `b2py`/`sdbk`/`utds` line protocol is structurally identical to a 3D-printer host→controller stream (Marlin `G1 X10 Y20` / `M114` replies). Both are: (a) line-delimited ASCII, (b) command-then-position-update verb pattern, (c) busy/idle state exposed via a single flag (`is_input_avail` ≈ "printer ready"), (d) explicit `__stop__` emergency stop. The "stop all" button + `interupt()` is the printer's `M112` kill command. Both work because the worker is single-threaded and exposed over a simple transport. The analogy breaks: printers have *real-time* constraints (thermal runaway) the SD pipeline doesn't.

- **GIMP / Photoshop Filter API ↔ plugin hook system**: `BasePlugin` + reflection-based `hook_*` discovery is the **GIMP PDB (procedure database) / Photoshop Filter API / Lightroom module** model. The `frame_interpolator.py` "plugin that calls a generator plugin" is the **plugin-of-plugins** pattern from GIMP's Script-Fu cascading filters. The `applets.py` registry is a coarser-grained *application-extension* registry (think Firefox `webExtension` API or VS Code's `contributes.commands`), where each applet is a top-level entry that registers its own inputs/outputs — not a chain of per-call hooks.

- **App Store / Steam Library ↔ ModelStore.vue**: The two-pane "Installed / Browse" pattern, card with image + title + subtitle + action button, the `model_metadata_to_str` rendering of `sd_type float16` (the app's "Version 1.4.2 · 124 MB"), the `Refresh` button at the bottom, the `Import From Computer` button as the "sideload / Install from APK" affordance — every UI element maps. The `model_selection.js` scoring + `pickOptimalOnboardingModel` is the App Store's "Suggested for You" algorithm from the era before ML-driven recommendation (hand-curated, keyword-weighted). The analogy breaks: this is a *closed catalog* from one provider (`models.diffusionbee.com`), not a marketplace with multiple developers uploading.

- **Package manager (apt / npm) ↔ dual install path**: `install_hf_model.py` (Python CLI, `huggingface_hub`) and `AssetsManager.vue` (Electron renderer, `request` + MD5 + tdict post-process) both produce the same registry state. The `setup_models.py` is the equivalent of `apt-get install` (declarative, idempotent, with `force` and `verify-only` flags). The `bundle_default_models.js` + `seed_bundled_models.js` is the equivalent of a Debian "preinst" hook + the `dpkg` `status` file. The `force` flag in `ensure_asset` is `apt-get install --reinstall`. The `delete_origional_always` is `apt-get clean`-after-install. The analogy breaks: there's no dependency resolution, no version graph, and the catalog is a flat list.

- **Render farm / batch render ↔ `SDManager.vue`**: Single-worker FIFO with a "ready" watcher that pulls the next job — exactly how a render farm's dispatcher works (each render node pulls from a shared queue when ready). The `seed += 1234` is the "vary the noise seed for the same camera" pattern from a 3D artist's batch render. The `on_img` callback to the gallery is the render farm's "chunk finished" callback. The `stop_all` flushing the queue is the farm's "user pressed stop, drain the rest." The analogy breaks: a real render farm has multiple workers and node-level health monitoring; this is single-worker.

- **WordPress / plugin CMS ↔ applets.py + plugin_system.py**: A single `register_applet` / `add_plugin` call attaches user-defined behavior at well-known extension points (`hook_pre_*/post_*`). The class is auto-discovered via reflection. The "applet" abstraction is the **page/post type** in WordPress — a top-level content type that registers its own UI inputs. The `forms/sample_ext_applet.json` is the page template. The analogy breaks: there's no per-request hook resolution, no priority, no conditional loading.

- **Cartridge / ROM image format ↔ `.tdict`**: A header with a magic number, a versioned ABI, a payload index (`keys_info`), and aligned data blocks. `ctdict_version` is the mapper version (NES MMC chip). The `tdict_format_version=2` / `min_supported_version=2` pair is the BIOS compat field. The pre-baked `causal_mask`, `aux_output_conv.weight`, and the `ff._split_*` tensors are equivalent to the iNES `mapper` field: the file is *prepared* for a specific consumer. The `init_read` legacy-detection error ("42, 10, 8, 42") is the equivalent of "this ROM uses a mapper this emulator doesn't support."

- **Visual regression test (Percy.io / Chromatic) ↔ `compose_txt2img_screenshot.py` + `verify_doc_screenshots.py`**: Generate a real artifact → composite into a UI screenshot → compute pixel statistics on the artifact region → fail if the region is "blank" (low variance). This is the **"visual diff" pattern, but with pixel-variance as the cheap diff signal**. The `region_variance` function is a poor-person's perceptual hash. The `prepare_doc_screenshots.py --generate` flag is the equivalent of `--update-snapshots` in Jest snapshot tests.

- **Personal access token (GitHub PAT / AWS keys) ↔ `hf_auth.js`**: Read from env, never persisted, cached only in process memory (`cachedToken` module variable), gates specific features (gated models), cleared on logout (`clearHfTokenCache`). The token is fetched *synchronously* from the main process and *synchronously* propagated to the renderer for the catalog merge. The analogy breaks: no expiry, no rotation, no scope system.

## Bridging Concepts

- **"Pre-linked executable"**: This concept from systems programming (linker turns `.o` + `.o` into `.exe`) bridges `.tdict` (post-`convert_model.py`, with `ff._split_*` and baked-in constants) to the SD generation loop (which can `read_key` from a known schema without runtime shape inference). The "shape constants" in `sd_shapes.py` are the equivalent of a linker script: they pre-assert the output binary's layout, eliminating the symbol-resolution step at runtime.

- **"Trust path"**: This concept from software supply chain security bridges the **two download paths** (SD's `request`+MD5 vs FLUX.2's `huggingface_hub`+Bearer) to the **two install verification paths** (tdict post-process vs `skip_checksum: true`). Closed ecosystem = content-hash trust (you don't need to trust the source because you can verify the bytes). Open ecosystem = authentication trust (you trust the source because they have a token, and the bytes are too big to hash on every install). The `delete_origional_always` flag is "verify-then-delete" — the package-manager equivalent of "don't keep the installer once the app is installed."

- **"Spool / render farm dispatcher"**: This concept bridges `SDManager.vue` (the dispatcher), `StableDiffusion.vue` (the worker), and the `b2py`/`sdbk` protocol (the transport). The `is_input_avail` boolean is the "node ready" heartbeat. The `__stop__` line is the emergency-stop command. The `on_img` callback is the "chunk finished" notification. The `get_and_do_job` recursion with `groups_todo.shift()` is the render farm's "pull next job" loop.

- **"Line protocol with verb prefixes"**: This concept (from IRC, SMTP, NNTP, G-code, serial terminals) bridges the `b2py`/`sdbk`/`utds`/`adlg`/`alrt` prefixes to the `___U_P_D_A_T_E___` path-delimiter to the `__converted_model_data__{...}` structured-footer pattern. All three are *escape-the-protocol* tricks: when the underlying transport is "lines of text," you invent a delimiter and call the result structured. The `Unbuffered` wrapper in `diffusionbee_backend.py:76` is the "raw terminal" hack that makes the whole thing work.

- **"Hand-tuned ranking function"**: This concept (from early search engines, the IMDb top-250 formula, App Store editorial picks, pre-ML recommendation systems) bridges `model_selection.js` (SD/FLUX.2 model ranking) to `ollama_prompt_service.js` (Ollama model ranking) to the `scoreStableDiffusionModel` vs `scoreFlux2Model` split. It's the same function, ported, with a different lexicon. The fact that it's duplicated (not abstracted) reveals a *missing generalization layer* — like having two near-identical shell scripts in two projects because nobody wrote `libcommon`.

- **"Bundled default / preinstalled app"**: This concept (from Docker base images, Android PreInstalledApp, Chrome's default search engine, iPhone's first-run apps) bridges `scripts/bundle_default_models.js` (build-time stager) → `electron_app/src/seed_bundled_models.js` (first-launch seeder) → `is_stock_model: true` flag → `model_selection.js` preference → `Homepage.vue` "Set up automatically" onboarding banner. The whole pipeline exists to make the very first `b2py t2im` work without a 2GB download. It's the *first 30 seconds* problem of every installable ML app.

- **"Single-binary CLI / daemon dual mode"**: This concept (from `git`, `docker`, `kubectl`, `tmux`) bridges `diffusionbee_backend.py`'s two argv modes to the spawn logic in `native_functions.js:495` (one-shot `convert_model` invocation) vs the persistent stdin/stdout consumer in `StableDiffusion.vue`. Same Python interpreter, two completely different lifecycles.

- **"First-run onboarding / 'featured' shelf"**: This concept (from every app store, Spotify Discover Weekly, Apple first-run iPhone setup, classic shareware "what's new" screens) bridges Homepage.vue's welcome tile carousel + "Set up automatically" button to the `Inspiration samples` heading + click-to-use prompt handler to the `recent_home_prompts` localStorage. The home page is a *gallery shop window* — show the user what's possible, one-click to try it.

- **"Reflection-based plugin discovery"**: This concept (from WordPress `do_action`, Eclipse extension points, VS Code `contributes.*`, Python `__init_subclass__`, GIMP PDB) bridges `backends/stable_diffusion/stable_diffusion/plugins/plugin_system.py`'s `dir(plugin_object)` walk to `backends/stable_diffusion/applets/applets.py`'s `register_applet` dict to the `applets/options.py` form JSON string. All three are *convention-over-configuration* extension points where naming patterns (`hook_*`, `applet_name = "..."`) replace explicit registration code.

- **"Cross-cutting localization with a single boolean"**: This concept (from "RTL toggle" patterns in bilingual UIs, the `isArabic` flag in `Homepage.vue`) bridges every `{{ app.app_state.isArabic ? 'النماذج' : 'Models' }}` template to the single `app_state.isArabic = !app.app_state.isArabic` toggle to the `lang-toggle` button at the bottom of the home page. It's the *fork-in-the-template* pattern that emerges whenever a codebase adds bilingual support late. The `i18n.js` table at `sdapplet.*` keys is the *next* layer of this same concept — still hand-rolled, not extracted to gettext.

## Productive Metaphors

- **The codebase is a printer**: Generation is to DiffusionBee as 3D printing is to a Marlin-controlled printer. The `b2py t2im` line is the sliced G-code; the backend's `process_opt` is the printer's main loop; `sdbk nwim` is the layer-complete notification; `__stop__` is the `M112` emergency stop; `interupt()` is the operator pressing the red button; `SDManager.vue` is the spool of pending jobs; the `is_input_avail` boolean is the "ready to receive next command" flag. The mental model is *fully* transferrable: a "render farm" is just a rack of printers sharing a spool.

- **The codebase is a package manager**: `install_hf_model.py` is to DiffusionBee as `apt` is to Debian. `downloaded_assets.json` is the `dpkg status` file. `bundle_default_models.js` is the "preinst" hook. `seed_bundled_models.js` is the "first-boot" hook. The catalog at `models.diffusionbee.com` is the "apt sources.list." The `force` flag is `apt-get install --reinstall`. The MD5 check is the `Release.gpg` signature. The `delete_origional_always` is `apt-get clean`. The two install paths (UI vs CLI) are `apt` and `aptitude` — different UIs, same database.

- **The codebase is a CMS**: The applets + plugins system is to DiffusionBee as WordPress is to a blog. `applets.py` is the "post type" registry. `plugin_system.py` is the `add_action`/`add_filter` model. `frame_interpolator.py` is a custom plugin. `forms/sample_ext_applet.json` is a page template. The "registered_ext_applets" dictionary in `App.vue:860` is the `wp_posts` table. The `home_category: "misc"` / `home_category: "pages"` partition is the WP `category` taxonomy. The `sidebar_show: "always" | "never"` is the WP `menu_position`.

- **The model store is a vinyl shop**: The two-pane "My Models / Available Models" split, the `title`/`description`/`model_meta_data` cards, the `Import From Computer` button (the "bring your own record" bin) — this is **Discogs + Bandcamp + a small record store's listening station**. The hand-curated welcome tile carousel on the homepage is the "now spinning" wall. The `pickOptimalOnboardingModel` is the clerk's "if you like X, you'll love Y" recommendation.

- **`.tdict` is a ROM cartridge**: The `magic_number`, the `tdict_format_version`, the `ctdict_version` ABI tag, the 64-byte block alignment, the pre-baked `causal_mask` constants — this is an **iNES ROM header** (mapper, PRG-ROM banks, CHR-ROM banks). The conversion step (`convert_model.py`) is the *mapper-specific preprocessing* (splitting `ff._split_*`, normalizing key names). The `init_read` legacy-detection error is the "this ROM uses a mapper this emulator doesn't support" error.

- **The doc screenshot pipeline is a visual CI test**: `prepare_doc_screenshots.py` is to docs what `jest --update-snapshots` is to a frontend repo. `compose_txt2img_screenshot.py` is the `page.screenshot()` step. `verify_doc_screenshots.py`'s `region_variance` is the snapshot diff. `ensure_doc_screenshots.sh` is the GitHub Actions workflow. The screenshots *are* the tests — they just happen to also be the documentation.

- **The line protocol is a serial terminal conversation**: `b2py` (browser→python) is the host's TX line. `sdbk` (stable-diffusion backend → kernel) is the controller's RX. The five prefixes are control characters. The `___U_P_D_A_T_E___` delimiter is a record separator. The `Unbuffered` stdout wrapper is the `stty raw` equivalent. The whole protocol works because both ends agreed on "we speak lines, not records."

- **The model selection scoring is a record-store clerk's mental algorithm**: "If you liked `juggernaut`, you'll love `realvis`; if you wanted `flux2_klein_4b` and have 16GB, here you go; if you have a Mac, here's a small bonus." It's a *human-curated decision tree* — exactly the kind of "no ML, just good taste" ranking that indie record stores use to hand you a record when you walk in and say "I liked X."

## Strongest Connections (ranked)

1. **`.tdict` as a pre-linked executable / NES ROM cartridge** — The format's magic number, versioned ABI, fixed block alignment, and pre-baked constants make this *unmistakably* the systems-programming "pre-linked binary" pattern. The analogy holds end-to-end: there's a "mapper version" (`ctdict_version`), a "BIOS compat" field (`min_supported_version`), a "preprocessing step" (`convert_model.py`'s `ff._split_*` and baked-in `causal_mask`), and a "legacy error" in `init_read`. The "breaks" are minimal — `.tdict` is more like an object file than a true executable, and the "linker" is the inference engine that reads keys by name.

2. **Generation queue as printer spool / single-worker render farm** — `SDManager.vue` is *exactly* a render-farm dispatcher: FIFO with a current-job pointer, a "ready" watcher, a stop-all that flushes the queue, an `is_input_avail` heartbeat, and a `seed += 1234` batch-vary pattern. The `__stop__` emergency-stop and the polling-stdin pattern reinforce this. The analogy breaks: there's no node health monitoring and no multi-worker dispatch.

3. **Dual install path as package manager (`apt` vs `dpkg`)** — `install_hf_model.py` (Python CLI) and `AssetsManager.vue` (Electron UI) converging on the same `downloaded_assets.json` registry is the textbook `apt` / `aptitude` / `synaptic` relationship: different frontends, identical database. The MD5-vs-bearer trust split mirrors the PGP-signature-vs-HTTPS-cert trust split. The `force` flag, `delete_origional_always`, and `verify-only` modes are exact `apt-get` analogues.

4. **b2py/sdbk line protocol as IRC / SMTP / G-code** — Five verb-prefixed line types, `___U_P_D_A_T_E___` path delimiter, `__converted_model_data__{...}` structured footer, `Unbuffered` stdout forcing raw mode — this is a *complete* line protocol in the pre-modern sense. It maps 1:1 to IRC verbs, SMTP commands, and G-code moves. The analogy breaks: there's no formal spec, no negotiation, and the only "error" handling is `print("sdbk errr %s"%(str(e)))`.

5. **Plugin / applet system as WordPress + GIMP filter API** — Reflection-based `hook_*` discovery in `plugin_system.py` plus class-based `register_applet` in `applets.py` is the *WordPress `add_action` crossed with the GIMP PDB* model. The `forms/sample_ext_applet.json` is the page template, the `home_category: "misc"` is the taxonomy, and the `sidebar_show: "always" | "never"` is the menu position. The analogy breaks: there's no per-request hook resolution, no priority system, and no conditional loading.

---
**Timing**: Started 2026-07-22 04:22:00 · Finished 2026-07-22 04:32:00
