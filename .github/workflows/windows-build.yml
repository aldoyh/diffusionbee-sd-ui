name: Windows Build

on:
  push:
    branches: [master]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          # tensorflow==2.10.0 ships no cp311 wheels; match the AppVeyor build on Python 3.10.
          python-version: '3.10'

      - name: Install backend Python dependencies
        run: |
          pip install "numpy<2" "scipy<1.14" pyinstaller==5.13.2
          pip install -r backends/stable_diffusion/requirements.txt

      - name: Build backend with PyInstaller
        working-directory: backends/stable_diffusion
        run: pyinstaller diffusionbee_backend.spec

      - name: Download RealESRGAN Windows release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release download --repo xinntao/Real-ESRGAN-ncnn-vulkan --pattern "*windows*" --output realesrgan.zip
          Expand-Archive -Path realesrgan.zip -DestinationPath realesrgan-release

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install root dependencies
        run: npm install

      - name: Install electron_app dependencies
        working-directory: electron_app
        run: pnpm install

      - name: Stage backend for packaging
        run: npm run prepare:backend:win

      - name: Build Windows installer
        run: npm run build:win

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: electron_app/dist_electron/*.exe
          if-no-files-found: error
